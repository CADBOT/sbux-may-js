function add(x, y) {
  return x + y;
}

var timesCalled = 0;

function addWithCount(x, y) {
  //timesCalled = timesCalled + 1;
  //return x + y;
  //
  var result = x + y;
}

var nums = [1, 2, 20, 15]
var newNums = nums.map(function(x) {
    return x + 1;
}) 

var firstName = 'charlie'
var newString = firstName + '!'

var names = ['janet', 'bob', 'jill', 'henry']
var changedNames = names.map /// finish me






